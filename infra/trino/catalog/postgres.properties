# 源系统连接。账号必须是只读（readme 第 8 节 凭证层）
# 密码走环境变量，不写进文件
connector.name=postgresql
connection-url=jdbc:postgresql://source_pg:5432/olist
connection-user=agent_ro
connection-password=${ENV:SOURCE_RO_PASSWORD}
